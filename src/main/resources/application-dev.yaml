spring:
  datasource:
    url: jdbc:h2:mem:lavava2026
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true

server:
  port: 8080
  servlet:
    context-path: /api/v3
